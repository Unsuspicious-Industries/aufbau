# Coq verification subsystem 

In this folder we implement Coq parsing and typechecking scripts for a small subset of the languages supported by **Aufbau**. It allows us to have a trustworthy, formally verified oracle to match our completions against. 

It provides the OCaml backend to the `aufbau verify` command. 

