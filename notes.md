# Dev notes

## TODOS
 - Improve the spec and formal definitions to clarify the ideas that *types are constraints* 
 - Ype checking should be implemented deeper inside the parser so that the `MetaParser`level can retry on typefails
 